## Authority

Read `CONTEXT.md` for domain terms and `GUIDE.md` for the canonical grammar, CLI contract, Koka module boundary, and Codex plugin workflow before changing them.

## Validation

Run the core checks and public CLI seam with `./acceptance.sh`.

## Maintaining this file

Keep only project knowledge useful to almost every future agent session. Prefer pointers to authoritative files over copied mechanics.
